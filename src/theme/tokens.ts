import { ThemeOptions } from '@mui/material';

export const lightThemeTokens: ThemeOptions = {
  palette: {
    mode: 'light',
  },
};

export const darkThemeTokens: ThemeOptions = {
  palette: {
    mode: 'dark',
  },
};
